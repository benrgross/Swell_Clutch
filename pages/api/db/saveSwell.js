import axios from "axios";
import pisma from "../../../lib/prisma"

export default async function handler(req, res) {
  if (req.method === "POST") {
    try {
        const save = await 
    } catch (err) {
      console.log(err);
    }
  }
}
